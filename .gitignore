.DS_Store
.playwright-mcp/
.claude/settings.local.json

# Внутренние материалы Сбера — не место в публичном репозитории.
# Identica/ целиком: там брендбук Бюро и агенда с реальными ФИО спикеров.
# Экспортированные картинки кладём в assets/ — они попадают в git осознанно.
*.pptx
*.docx
*.pdf
Identica/

# Локальные файлы со ссылками и токенами — НЕ коммитить
LINKS.local.md
